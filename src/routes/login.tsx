import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { AuthPage } from "@/components/auth-page";
import { demoSession, setSession } from "@/lib/remargin";

export const Route = createFileRoute("/login")({ head:()=>({meta:[{title:"Login — ReMargin"},{name:"description",content:"Sign in to your ReMargin factory workspace."},{property:"og:title",content:"Login — ReMargin"},{property:"og:description",content:"Access your factory sustainability intelligence workspace."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}), component: Login });
function Login(){const nav=useNavigate();const [company,setCompany]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");function submit(e:React.FormEvent){e.preventDefault();if(company.trim().length<2||password.length<8){setError("Enter a valid company name and password of at least 8 characters.");return;}setSession({...demoSession,company});nav({to:"/dashboard/carbon"});}return <AuthPage title="Welcome back" subtitle="Sign in to continue monitoring factory performance."><form onSubmit={submit} className="space-y-5"><label className="block"><span className="form-label">Company name</span><input className="form-input" value={company} onChange={e=>setCompany(e.target.value)} placeholder="Precision Works India"/></label><label className="block"><span className="form-label">Password</span><input className="form-input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password"/></label>{error&&<p className="text-sm text-destructive">{error}</p>}<div className="flex justify-end"><button type="button" className="text-xs font-bold text-primary">Forgot password?</button></div><button className="button-primary w-full" type="submit">Login</button><p className="text-center text-sm text-muted-foreground">New to ReMargin? <Link to="/register" className="font-bold text-primary">Register</Link></p><p className="rounded-md bg-muted p-3 text-xs text-muted-foreground">Demo: enter any company name and a password with 8 or more characters.</p></form></AuthPage>}